{
  "name": "zaparomm",
  "version": "0.1.0",
  "description": "Launch RomM games on a local Zaparoo instance",
  "license": "0BSD",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "webextension-polyfill": "^0.12.0"
  },
  "devDependencies": {
    "@types/webextension-polyfill": "^0.12.1",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.3"
  }
}
